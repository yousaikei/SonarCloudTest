# SonarCloud 项目配置
# 注意：主要配置已通过 cloudbuild.yaml 中的命令行参数设置
# 这个文件可以保留用于额外的配置选项

# 项目名称和版本（可选）
sonar.projectName=HelloWorld Python
sonar.projectVersion=1.0

# 排除文件（可选）
sonar.exclusions=**/*_test.py,**/test_*.py,**/tests/**,**/__pycache__/**

# Python 特定配置（可选）
sonar.python.version=3.9